%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @people.each do |person|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(person.email.downcase)}?s=40")
            = link_to person.full_name, person
          %td.extra-padding= person.queue_items.count
          %td.extra-padding= person.followers.count
          %td.extra-padding
            = link_to "", method: :post do
              %i.glyphicon.glyphicon-remove
